/*

The struct type can be defined with tags, 
which provide additional information about how a field should be processed. 
Struct tags are just strings that are interpreted by the code that processes struct values,
using the features provided by the reflect package.
نوع ساختار را می توان با تگ هایی تعریف کرد
که اطلاعات بیشتری در مورد نحوه پردازش یک فیلد ارائه می دهد.
تگ‌های Struct فقط رشته‌هایی هستند که توسط کدی که مقادیر ساختار را پردازش می‌کند،
با استفاده از ویژگی‌های ارائه شده توسط بسته reflect تفسیر می‌شوند.













*/

package main