AN OVERVIEW OF NETWORK SYSTEM

A "computer network" is a connection between two or more devices, or "nodes", that allows each node to share data.

Choosing a Network Topology:
|    The organization of nodes in a network is called "topology".
|
|    Topology types fall into six basic categories:
|        1- point-to-point
|        2- daisy chain
|        3- bus
|        4- ring
|        5- star
|        6- mesh
|
|    1- point-to-point:
|        A direct connection between two nodes
|
|    2- daisy chain  زنجیره گل‌آفتاب‌گردان
|        A series of point-to-point connection creates a "daisy chain"
|        Intermediate nodes between an origin node and destination node are commonly known as "hops".
|
|    3- bus:
|        Wired bus networks aren't common, but this type of topology drives wireless networks.
|        The nodes on wired network see all he traffic
|
|    4- ring:
|        was used in some fiber-optic network deployment.
|        Traffic traveling is a single direction.
|        Slow.
|
|    5- star:
|        A central node has individual poin-to-point connections to all other nodes.
|        The central node is often a "network switch",
|        which is a device that accepts data from origin nodes and retransmits data to the destination nodes.
|
|    6- mesh:
|        Every node has direct connection to every other node.    

Banwidth VS. Latency: پهنای باند در مقابل تاخیر
|   Network "Banwidth" is the amount of data can send over a network connection in an interval of time. فاصله زمانی
|   Faster doesn't always mean greater performance. because of "Latency"
|   Network "Latency" is a measure of the time that passes between sending a network resource request and response.
|   displacement مفهومش همون سرعت جا به جایی هست پکت ها هست
|   
|   The network latency comes from:
|   |    1- The network latency between the client and server.
|   |    2- The time it takes to retrieve data from a data store.
|   |    3- The time it takes to compile dynamic content on the server side.
|   |    4- The time it takes for the web browser to render the page.
|
|   ###### Keeping latency to minimum while writing network sofware.
|   
|   To decrease Latency: 
|   1- reduce both the distance and the number of hops between users and your service by using a CDN.
|   2- Optimizing request/response sizes.
|   3- Incorporating a caching strategy in network application. 
|   4- Taking advantage of Go's concurrency to minimize server-side blocking of the response.


The Open Systems Interconnection(OSI) Reference Model: 
|   اتصال سیستم های باز
|    
|   In the 1917s. as computer networks became increasingly complex,
|   researchers created the Open Systems Interconnection(OSI) reference model to standardize networking.


The Hierarchal Layers of the OSI Reference Model:
|   سلسه مراتب لایه‌ها
|
|   Layer 7 application layer
|   Layer 6 presentation layer
|   Layer 5 session layer
|   Layer 4 transport layer
|   Layer 3 network layer
|   Layer 2 data link layer
|   Layer 1 phisical layer
|
|   Layer 7 application layer: Web browser, Instagram, Telegram etc
|
|   Layer 6 presentation layer: prepares data for the network layer.
|                               present data to the application layer.   
|
|   Layer 5 session layer: manages the connection life cycle between nodes on network.
|                          Some Layer 7 protocols rely on services provided by Layer 5.
|                          برخی از پروتکل های لایه 7 به خدمات ارائه شده توسط لایه 5 متکی هستند.
|                          
|   Layer 4 transport layer: Controls and coordinates the transfer of data between two nodes while maintaining the reliability of the transfer.
|                            کنترل و هماهنگی انتقال داده
|                            Controlling the speed of data transfer
|                                                     chunking or segmenting the data
|                            Often protocols in this layer might retransmit data if the recipient doesn't acknowledge receipt of the data.
|                            اگر گیرنده دریافت داده ها را تایید نکند، اغلب پروتکل های این لایه ممکن است داده ها را دوباره ارسال کنند.
|
|   Layer 3 network layer: Allows you to send data to a network address without having a direct point-to-point connection to the remote node.
|
|   Layer 2 data link layer: Handles data transfer between two directly connected nodes.
|                            facilitates data transfer from a computer to a switch and from the switch to another computer.
|                            انتقال داده ها از کامپیوتر به سوییچ و از سوییچ به کامپیوتر دیگر را تسهیل می کند.
|                            Protocols further up the network protocol stack can ensure that 
|                            the data transmission is reliable is this layer doesn't do so, though generally with less efficiency.
|                            پروتکل‌های بالاتر از پشته پروتکل شبکه می‌توانند اطمینان حاصل کنند که انتقال داده قابل اعتماد است،
|                            در صورتی که این لایه این کار را انجام نمی‌دهد، هرچند عموما با کارایی کمتر.
|
|   Layer 1 phisical layer: converts bits from the network stack to electrical.
|                           بیت ها را از پشته شبکه به الکتریکی تبدیل می کند.
|                           converts bits from the network stack to optic.
|                           Controls the bit rate.
|                           The bit rate is the data speed limit.



Sending Traffic by Using Data Encapsulation:
|   ارسال ترافیک با استفاده از کپسوله سازی داده ها
|   
|   Encapsulation is a method of hiding implementation details of making only relevant details available to the recipient.
|   کپسوله‌سازی روشی برای پنهان کردن جزئیات پیاده‌سازی و در دسترس قرار دادن جزییات فقط به گیرنده می باشد.                        
|   
|   As data travels down the stack, it's encapsulated by the layer below.
|   
|   We typically call the data traveling down the stack a "payload", although you might see it referred to as a "message body".
|   معمولاً داده‌هایی را که به سمت پایین پشته حرکت می‌کنند، «بارگذاری» می‌نامیم، اگرچه ممکن است آن را «بدنه پیام» ببینید.
|   
|   The literature uses the term "service data unit" SDU.
|
|   Even protocols that operate in a single OSI layer use data encapsulation.
|   
|   Communication between the client and the server on the same layer is called "Horizontal Communication".
|   ارتباط بین مشتری و سرور در یک لایه "ارتباط افقی" نامیده می شود.
|   
|   TCP is a Layer 4 protocol whose payloads are also known as "segments" or "datagrams".
|   
|   TCP accepts Layer 5’s payload and adds its header before send-ing the segment on to Layer 3.
|   
|   The Internet Protocol (IP) at Layer 3 receives the TCP segment and encapsulates it with a header to create Layer 3’s payload, which is known as a "packet".
|
|   Layer 2 accepts the packet and adds a header and a footer, creating its payload, called a "frame".
|   
|   Layer 2’s header translates the recipient’s IP address into a media access control (MAC) address, which is a unique identifier assigned to the node’s network interface.
|   هدر لایه 2 آدرس IP گیرنده را به یک آدرس کنترل دسترسی رسانه (MAC) ترجمه می کند، که یک شناسه منحصر به فرد است که به رابط شبکه گره اختصاص داده شده است.
|   
|   Its footer contains a frame check sequence (FCS), which is a checksum to facilitate error detection. 
|   پاورقی آن حاوی یک دنباله چک فریم (FCS) است که یک جمع کنترلی برای تسهیل تشخیص خطا است.
|   
|   Layer 1 receives Layer 2’s payload in the form of bits and sends the bits to the server.
|   
|   The server’s Layer 1 receives the bits, converts them to a frame, and sends the frame up to Layer 2.
|   
|   Layer 2 strips its header and footer from the frame and passes the packet up to Layer 3. 
|   
|   The process of reversing each layer’s encapsulation continues up the stack until the payload reaches Layer 7.
|   روند معکوس کردن کپسوله کردن هر لایه تا زمانی که بار به لایه 7 برسد در پشته ادامه می یابد.
|   
|   Finally, the HTTP server receives the client’s request from the net-work stack.
|   در نهایت، سرور HTTP درخواست مشتری را از پشته شبکه دریافت می کند.


The TCP/IP Model:
|   Around the same time as researchers were developing the OSI reference model, 
|   the Defense Advanced Research Projects Agency (DARPA), 
|   an agency of the US Department of Defense, spearheaded a parallel effort to develop protocols. 
|   This effort resulted in a set of  protocols we now call the  TCP/IP model. 
|   The project’s impact on the US military, and subsequently the world’s communications, was profound.  
|   he TCP/IP model reached critical mass when Microsoft incorporated it  nto Windows 95 in the early 1990s. 
|   Today, TCP/IP is ubiquitous in  omputer networking, and it’s the protocol stack we’ll use in this book.
|   
|   TCP/IP’s application and link layers generalize their OSI counterparts.
|   
|   The TCP/IP model simplifies OSI’s application, presentation, and ses-sion layers into a single application layer, primarily because TCP/IP’s proto-cols frequently cross boundaries of OSI Layers 5 through 7. 
|
|   Likewise, OSI’s data link and physical layers correspond to TCP/IP’s link layer.
|   
|   TCP/IP’s and OSI’s transport and network layers share a one-to-one relationship.


The Application Layer:
|   You’ll notice that the TCP/IP application layer encompasses three OSI layers.
|   This is because TCP/IP doesn’t define specific presentation or ses-sion functions.
|   Instead, the specific application protocol implementations concern themselves with those details. 
|
|   Common TCP/IP application layer protocols include HTTP, the File Transfer Protocol (FTP) 
|   for file transfers between nodes, and the Simple Mail Transfer Protocol (SMTP) for sending email to mail servers. 
|   The Dynamic Host Configuration Protocol (DHCP) and the Domain Name System 
|   (DNS) also function in the application layer. 

The Transport Layer:
|   Transport layer protocols handle the transfer of data between two nodes, like OSI’s Layer 4. 
|   These protocols can help ensure data integrity by making sure that all data sent from the origin completely 
|   and correctly makes its way to the destination.
|   
|   Keep in mind that data integrity doesn’t mean the destina-tion will receive all segments we send through the transport layer. 
|   There are just too many causes of packet loss over a network
|   
|   handles segments, or datagrams.
|   
|   Most of our network applications rely on the transport layer protocols 
|   to handle the error correction, flow control, retransmission, and transport acknowledgment of each segment. 
|   اکثر برنامه های شبکه ما برای مدیریت تصحیح خطا، کنترل جریان، ارسال مجدد و تایید انتقال هر بخش به پروتکل های لایه انتقال متکی هستند.
|
|   If your application requires the use of UDP for maximal throughput, 
|   the onus is on you to implement some sort of error checking or session management, since UDP provides neither.

The Internet Layer:
|   The internet layer is responsible for routing packets of data from the upper layers between the origin node and the destination node, 
|   often over  multiple networks with heterogeneous physical media.
|   
|   Internet Protocol version 4 (IPv4), 
|   Internet Protocol version 6 (IPv6), 
|   Border Gateway Protocol (BGP), 
|   Internet Control Message Protocol (ICMP), 
|   Internet Group Management Protocol (IGMP), 
|   and the Internet Protocol Security (IPsec) suite, 
|   among others, provide host identification and routing to TCP/IP’s internet layer.
|
|   know that this layer plays an integral role in ensuring that the data we send reaches its destination, 
|   no matter the complexity between the origin and destination.
|   بدانید که این لایه در حصول اطمینان از اینکه داده هایی که ارسال می کنیم به مقصد می رسند،
|   بدون توجه به پیچیدگی بین مبدا و مقصد، نقش اساسی دارد.


The Link Layer:
|   The link layer, which corresponds to Layers 1 and 2 of the OSI reference model, 
|   is the interface between the core TCP/IP protocols and the physical media.
|   
|   The link layer’s Address Resolution Protocol (ARP) translates a node’s  IP address to the MAC address of its network interface.
|   
|   The link layer embeds the MAC address in each frame’s header before passing the frame on to the physical network. 
|   
|   Not all TCP/IP implementations include link layer protocols. 


What You’ve Learned:
|   You learned about common network topologies.
|   
|   how to combine those topologies to maximize their advantages and minimize their disadvantages.
|   چگونه می توان آن توپولوژی ها را برای به حداکثر رساندن مزایا و به حداقل رساندن معایب آنها ترکیب کرد.
|   
|   You also learned about the OSI and TCP/IP reference models, their layering, and data encapsulation. 
|   
|   However, it’s important that you explore these topics in greater depth, 
|   because comprehensive knowledge of net-working principles and architectures can help you devise better algorithms.
|   با این حال، مهم است که این موضوعات را با عمق بیشتری بررسی کنید، 
|   زیرا دانش جامع اصول و معماری شبکه می تواند به شما در ایجاد الگوریتم های بهتر کمک کند.
|   
|   I also recommend you revisit this chapter after working through some of the examples in this book.
|   همچنین توصیه می‌کنم پس از بررسی برخی از مثال‌های این کتاب، این فصل را دوباره مرور کنید.
|   
|   The OSI reference model is available for reading online at:
|   https://www.itu.int/rec/T-REC-X.200-199407-I/en/
|
|   Two Requests for Comments (RFCs)
|   —detailed publications meant to describe internet technologies—
|   outline the TCP/IP reference model: RFC 1122 and RFC 1123:
|   https://tools.ietf.org/html/rfc1122/   OR https://datatracker.ietf.org/doc/html/rfc1122/ 
|   and 
|   https://tools.ietf.org/html/rfc1123/   OR https://datatracker.ietf.org/doc/html/rfc1123/
|   
|   RFC 1122 covers the lower three layers of the TCP/IP model, 
|   whereas RFC 1123 describes the applica-tion layer and support protocols, such as DNS.
|   
|   Network latency has plagued countless network applications and spawned an industry. 
|   تأخیر شبکه، برنامه‌های شبکه‌ای بی‌شماری را آزار داده و صنعت را به وجود آورده است.
|   
|   CDN blogs that provide insightful posts include the Cloudflare Blog:
|   https://blog.cloudflare.com/
|   
|   the KeyCDN Blog:
|   https://www.keycdn.com
|   
|   the Fastly Blog:
|   https://www.fastly.com/blog/
|   
|   “Latency (engineering)” on Wikipedia:
|   https://en.wikipedia.org/wiki/Latency_(engineering)
|   
|   Cloudflare’s glossary:
|   https://www.cloudflare.com/learning/performance/glossary/what-is-latency/
|   
|   
|   
|   






















































