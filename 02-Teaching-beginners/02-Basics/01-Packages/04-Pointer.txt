

در قسمت زیر خروجی کدها نوشته شده است


fmt.Println("language =", language)    // output->      language = golang
fmt.Println("pointer =", pointer)      // output->      pointer = 0xc000010250
fmt.Println("*pointer =", *pointer)    // output->      *pointer = golang}
